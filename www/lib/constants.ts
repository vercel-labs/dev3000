/**
 * Production URL for the dev3000 website
 * Use this constant instead of hardcoding the URL
 */
export const DEV3000_URL = "https://dev3000.ai"

/**
 * Production API base URL
 * This is the same as DEV3000_URL but explicitly named for API usage
 */
export const DEV3000_API_URL = DEV3000_URL
