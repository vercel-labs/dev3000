# React Performance Example

Purpose: Trigger the `react-performance` workflow with realistic performance issues.

Expected fixes (examples):
- Parallelize independent server data fetches.
- Memoize or move expensive client computations.
- Avoid rebuilding large arrays on every render.
